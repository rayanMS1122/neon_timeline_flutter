name: neon_timeline_flutter_example
description: Searchable 1.x-16.0.0 showcase for the compact planner and preserved legacy views.
publish_to: none
version: 16.0.0
environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  neon_timeline_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
