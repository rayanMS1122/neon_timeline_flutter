# Validation report — 14.1.1

## Applied correction

All v14 declarations using `ValueListenable` now have explicit Flutter foundation visibility.

## Static checks performed

- 245 Dart files parsed with the Dart tree-sitter grammar;
- 0 files with syntax errors;
- 0 missing relative Dart imports, exports or parts;
- 0 v14 `ValueListenable` declarations without a direct foundation import;
- package version: `14.1.1`;
- example version: `14.1.1`;
- patch reproduction and archive integrity verified after packaging.

Flutter and Dart SDK executables are not installed in this environment. No successful `flutter analyze`, `flutter test`, or Chrome build is claimed.
